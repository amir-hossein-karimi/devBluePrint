import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  container: {
    // changeBackgroundColor will change
    backgroundColor: changeBackgroundColor,
  },
}));

export default useStyles;
